/**
 * Developer HUD (`.` key) — live `devHud` sliders (plan P9.2). Stub until UI is built.
 */

/**
 * @typedef {{ dispose(): void }} DevHudController
 */

/**
 * @returns {DevHudController}
 */
export function createDevHudController() {
  return {
    dispose() {},
  };
}
