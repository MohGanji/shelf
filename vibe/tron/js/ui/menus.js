/**
 * Pause menu, controls overlay, settings (plan P7.5–P7.6). Stub until state machine lands.
 */

/**
 * @typedef {{ dispose(): void }} MenuController
 */

/**
 * @returns {MenuController}
 */
export function createMenusController() {
  return {
    dispose() {},
  };
}
